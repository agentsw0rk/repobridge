interface:
  display_name: "RepoBridge Project Context"
  short_description: "Search dependency sources, symbols, routes, structure, and graph edges with RepoBridge"
  default_prompt: "Use $repobridge for dependency or fetched source-code investigation. Resolve specs with RepoBridge and use repobridge context, explore, search, route-aware queries, contains structure traversal, and graph commands before any raw rg, grep, network fetch, or manual source-tree browsing."
