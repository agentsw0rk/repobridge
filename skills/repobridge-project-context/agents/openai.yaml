interface:
  display_name: "RepoBridge Project Context"
  short_description: "Fetch and search dependency sources"
  default_prompt: "Use $repobridge-project-context to scan this project, fetch relevant dependency sources with RepoBridge, and search their AST code graphs as read-only reference context."
